{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    "group:all",
    "schedule:weekly",
    ":semanticCommitTypeAll(chore)"
  ],
  "mode": "full",
  "packageRules": [
    {
      "matchPackageNames": ["nock"],
      "matchUpdateTypes": ["major"],
      "enabled": false,
      "description": "nock 14 changes network interception behavior and breaks tool-cache downloads in tests (requests abort, HTTP retry loops). Pinned to 13.x."
    }
  ]
}
